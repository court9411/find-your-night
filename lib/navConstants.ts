// Single source of truth for bottom-nav sizing so BottomNav, the FAB, and
// AppShell's content spacer never drift out of sync with each other.
export const BOTTOM_NAV_CONTENT_HEIGHT = 64; // px, excludes safe-area inset
export const TAB_ROUTES = ["/results", "/map", "/profile"] as const;
